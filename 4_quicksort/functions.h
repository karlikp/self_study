#pragma once
#include <iostream>
#include <array>
#include <vector>
#include <list>

using namespace std;

int sum(vector <int> arr);
int counter(list<int> lista);
int max_element(list<int> roll, int compare_max);
